# 长期的伙伴：让龙虾拥有长期记忆

OpenClaw 的 MEMORY.md 机制是一种「被动记忆」——文件持久化，但龙虾不会主动判断什么值得记住。

我想要「主动记忆」——龙虾自己判断什么该记，自动保存，下次见面还能想起来。

这篇文章记录了给龙虾装上长期记忆的方案。

---

## 一、为什么需要长期记忆

即使配置了 14 天空闲重置，仍然有问题：

1. 对话崩了，`/new` 新建会话，龙虾不记得之前聊了什么
2. 很久前提的事，龙虾早就忘了
3. 用户偏好散落在各次对话中，无法整合

**一句话**：没有长期记忆，每次对话都是从零开始。

---

## 二、方案选型

Google 的 [Always On Memory Agent](https://github.com/GoogleCloudPlatform/generative-ai/tree/main/gemini/agents/always-on-memory-agent) 提供了一个思路：**模拟人脑的记忆整合机制**——睡眠时回放经历，发现关联，压缩成稳定记忆。

GitHub 上有几个现成方案：

| 项目 | Stars | 特点 |
|-----|-------|------|
| mem0 | 49k+ | Y Combinator 孵化，通用记忆层 |
| memU | 12k+ | **专为 OpenClaw 设计** |
| Letta | 21k+ | 前身 MemGPT，自进化记忆 |
| memvid | 13k+ | 单文件存储，无需数据库 |

**选择 memU**：README 直接提到 OpenClaw，专为 24/7 运行的 Agent 设计，支持主动记忆机制。

---

## 三、memU 原理

memU 的核心是「记忆整合」：

```
对话 → LLM 提取记忆项 → Embedding 向量化 → PostgreSQL 存储
                                    ↓
                         需要时向量相似度搜索
```

**三个组件协作**：

1. **LLM（Chat API）**：从对话中提取记忆项（偏好、事实、目标、事件）
2. **Embedding 模型**：将记忆项向量化，用于相似度搜索
3. **PostgreSQL + pgvector**：持久化存储，支持向量索引

---

## 四、实施方案

### 4.1 架构

```
OpenClaw 主会话
    │
    ├─▶ MEMORY.md（核心记忆，手动维护）
    │
    └─▶ sessions/*.jsonl（对话记录）
            │
            ▼ memUbot 守护进程（每 10 秒检查）
        长期记忆系统
            │
            ├─ 腾讯云 GLM-5：提取记忆项
            │
            ├─ 本地 bge-small-zh-v1.5：向量化（~0.1秒）
            │
            └─ PostgreSQL + pgvector：存储
```

**选择理由**：
- **Chat API 用腾讯云**：GLM-5 延迟约 1 秒，可接受
- **Embedding 用本地模型**：免费、快速（~0.1秒）、中文效果好
- **PostgreSQL + pgvector**：官方推荐，支持向量索引

### 4.2 memUbot 守护进程

memUbot 是一个 systemd 服务，每 10 秒检查 OpenClaw 的会话文件：

```bash
# 启动服务
systemctl start memubot

# 查看状态
systemctl status memubot

# 查看日志
journalctl -u memubot -f
```

**工作流程**：

1. 每 10 秒扫描 `~/.openclaw/agents/main/sessions/*.jsonl`
2. 找到新的 user-assistant 对话
3. 调用 GLM-5 提取记忆项（偏好、事实等）
4. 本地 Embedding 向量化
5. 存入 PostgreSQL（去重：相似度 >90% 跳过）
6. 更新同步状态

**去重机制**：
- 启动时加载已同步的消息 ID
- 循环中持续更新，避免重复同步
- 存储时检查向量相似度，>90% 视为重复

### 4.3 新会话自动加载记忆

在 `AGENTS.md` 中配置：

```markdown
## Every Session

4. **查询最近 10 条记忆**：执行 `python local_memory.py recent 10`
```

**效果**：新会话自动了解用户偏好和历史，无需手动查询。

---

## 五、效果验证

以下是 memU 自动提取的真实记忆示例：

### 场景一：用户背景记忆

**对话中提到**：*我有 Java 背景，熟悉命令行*

**新会话问**：我的技术背景是什么？

**memU 返回**：用户有 Java 背景，熟悉命令行

✅ 背景信息跨会话保留

### 场景二：工作信息记忆

**对话中提到**：*我从事开发工作*

**新会话问**：我的工作背景？

**memU 返回**：用户从事开发工作

✅ 工作信息跨会话保留

### 场景三：项目上下文记忆

**对话中提到**：*我在做 AI Agent 持久化记忆系统*

**新会话问**：我最近在做什么项目？

**memU 返回**：用户正在开发 AI Agent 持久化记忆系统

✅ 项目信息跨会话可用

### 场景四：偏好记忆

**对话中提到**：*你独立解决问题，自己能解决的事绝不询问用户*

**新会话问**：我对助手有什么要求？

**memU 返回**：用户要求助手独立解决问题，仅在需要私密信息时才询问

✅ 偏好自动从对话中提取

---

## 六、写在最后

理想状态是：每次对话结束自动判断是否有价值记忆，每次对话开始自动注入相关记忆。这需要魔改 OpenClaw 核心，暂时用守护进程 + 新会话加载的方式。

**但已经足够好用**——龙虾不再是"金鱼记忆"，而是能记住偏好、项目、重要事件。

> 🦐 **有了长期记忆的龙虾，才算真正的伙伴。**
